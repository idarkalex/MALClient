<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:MALPlus.Views"
       x:Class="MALPlus.AppShell"
       Shell.NavBarIsVisible="False">
    <TabBar>
        <ShellContent Title="Anime" Route="anime" ContentTemplate="{DataTemplate views:AnimeListPage}" />
        <ShellContent Title="Manga" Route="manga" ContentTemplate="{DataTemplate views:PlaceholderPage}" />
        <ShellContent Title="Discover" Route="discover" ContentTemplate="{DataTemplate views:PlaceholderPage}" />
        <ShellContent Title="More" Route="more" ContentTemplate="{DataTemplate views:PlaceholderPage}" />
    </TabBar>
</Shell>